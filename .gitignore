node_modules/
dist/
coverage/
*.log
*.tgz
.DS_Store
.env
.env.local

# Project-local polyglot config can carry an inline apiKey (see README's settings.json /
# multiple-models docs) - never commit it, including from self-testing this repo with itself.
.polyglot/

# Generated at pack time from the root README.md (scripts/gen-cli-readme.mjs).
packages/cli/README.md

# Local output of `pnpm scenario:live` - failure transcripts and the per-run summary log.
packages/core/src/testing/captured-failures/
scenario-results.jsonl
