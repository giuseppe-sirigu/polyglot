node_modules/
dist/
coverage/
*.log
*.tgz
.DS_Store
.env
.env.local

# Project-local polyglot config can carry an inline apiKey (see README's settings.json /
# multiple-models docs) — never commit it, including from self-testing this repo with itself.
.polyglot/
